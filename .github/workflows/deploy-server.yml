name: Build and Deploy Unity Server

on:
  push:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    # 1️⃣  Descargar el repositorio
    - uses: actions/checkout@v3

    # 2️⃣  Activar Unity Personal (login con tu cuenta)
    - name: Activate Unity
      uses: game-ci/unity-activate@v2
      with:
        unityVersion: 2022.3.13f1            # ← tu versión oficial
      env:
        UNITY_EMAIL:    ${{ secrets.UNITY_EMAIL }}
        UNITY_PASSWORD: ${{ secrets.UNITY_PASSWORD }}

    # 3️⃣  Compilar el servidor (headless Linux)
    - name: Build headless Linux server
      uses: game-ci/unity-builder@v2
      with:
        unityVersion: 2022.3.13f1
        targetPlatform: StandaloneLinux64
        buildMethod: BuildScript.PerformServerBuild
      env:
        UNITY_EMAIL:    ${{ secrets.UNITY_EMAIL }}
        UNITY_PASSWORD: ${{ secrets.UNITY_PASSWORD }}

    # 4️⃣  Copiar TODO el build a tu VPS
    - name: Upload server build to VPS
      uses: appleboy/scp-action@v0.1.5
      with:
        host:     ${{ secrets.SERVER_IP }}
        username: ${{ secrets.SERVER_USER }}
        key:      ${{ secrets.SSH_PRIVATE_KEY }}
        source:   "buildServer/**"
        target:   "/home/${{ secrets.SERVER_USER }}/server"

    # 5️⃣  Reiniciar el servicio remoto
    - name: Restart remote game server
      uses: appleboy/ssh-action@v1.0.0
      with:
        host:     ${{ secrets.SERVER_IP }}
        username: ${{ secrets.SERVER_USER }}
        key:      ${{ secrets.SSH_PRIVATE_KEY }}
        script: |
          sudo systemctl restart mygame-server
